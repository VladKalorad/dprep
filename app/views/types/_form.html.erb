<%= form_with(model: type) do |form| %>
  <% if type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
        <% type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.collection_select :category_id, Category.order(:name), :id, :name, { prompt: 'Выберите категорию' }, { class: 'form-control' } %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
